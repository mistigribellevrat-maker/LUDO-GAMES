[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --cov=server_client
    --cov=avatars
    --cov=scoring
    --cov-report=term-missing
markers =
    slow: marks tests as slow
